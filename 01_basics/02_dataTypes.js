"use strict" // treat all JS code as newer version

// alert(3+3);
/* We are using node, not browser */

let name = "Samar"; //strig dataType

let age  = 23; //number dataType (range = 2 to power 53)

// bigInt

let isLoggedIn = true; //boolean dataType

let salary; // undefined it is an object
let work = null; //standalone value
console.log(typeof salary);


// symbol dataType